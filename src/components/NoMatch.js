import React from "react";

/// * is used to find the no match route

const NoMatch = () => {
  return (
    <div>
      <h1>Ohh your did not find the page!</h1>
    </div>
  );
};

export default NoMatch;
